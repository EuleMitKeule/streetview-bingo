[pytest]
log_cli = false
log_format = [%(asctime)s] %(levelname)s:    %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = --no-cov-on-fail --cov-config="config/.coveragerc"
filterwarnings = ignore::DeprecationWarning:flask_marshmallow
env =
    CONFIG_PATH = config/test.config.yml